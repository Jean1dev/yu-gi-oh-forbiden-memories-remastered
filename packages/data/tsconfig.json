{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Only the CLI adapter and the integration tests touch Node globals; the
    // ingestion core under src/ingestion stays free of them.
    "types": ["node"]
  },
  "include": ["src/**/*.ts", "scripts/**/*.ts", "tests/**/*.ts"]
}
